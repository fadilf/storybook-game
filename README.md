# Storybook Game

### Setup
1. Clone the repo
2. Create a virtual environment
3. Activate it, and run `pip install -r requirements.txt`
4. Create a `.env` file
5. Fill in `FAL_KEY` with a fal.ai API key
6. Fill in `GEMINI_API_KEY` with a Google Gemini API key
7. Run `streamlit run app.py`